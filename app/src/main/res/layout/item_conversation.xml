<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    card_view:cardCornerRadius="8dp"
    card_view:cardElevation="4dp"
    card_view:cardBackgroundColor="@color/lightBlue">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Conversation Icon -->
        <ImageView
            android:id="@+id/imageConversationIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_conversation"
            android:contentDescription="@string/conversation_icon_desc"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true" />

        <!-- Conversation Title -->
        <TextView
            android:id="@+id/textConversationTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Conversation Title"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_toEndOf="@id/imageConversationIcon"
            android:layout_marginStart="8dp"
            android:layout_alignTop="@id/imageConversationIcon" />

        <!-- Last Message -->
        <TextView
            android:id="@+id/textLastMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap to select conversation"
            android:textSize="14sp"
            android:textColor="#777"
            android:layout_below="@id/textConversationTitle"
            android:layout_marginTop="4dp"
            android:layout_toEndOf="@id/imageConversationIcon"
            android:layout_marginStart="8dp" />
    </RelativeLayout>

</androidx.cardview.widget.CardView>
